# Changelog

## v0.1.1 - 2026-05-10

### Added

- `now_in_milliseconds` helper in `gquery/lustre` for getting the current time in ms
- `stale_for` field on queries to support the `keepPreviousData` pattern
- `placeholder` support on queries, allowing stale data to be shown while a fresh fetch is in progress
